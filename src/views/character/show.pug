extends ../layout

block content
    if error 
        p(class="error")= error
    if character   
        h2=character.Name
        p=racename
        p=weaponName
        p=mapName
        

    a(href="/character")
        button Volver